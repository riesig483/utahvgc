# This is a plain static site (no Jekyll, no Gemfile/Bundler) -- nothing
# needs ignoring by default. In particular, vendor/ is NOT a Bundler gem
# cache here: it holds this app's own vendored library
# (vendor/html2canvas.min.js), which must stay tracked.
